import type {
  ContentPadding,
  ContentWidth,
} from "@shurokkha/ui-patterns/layout"

export const docsShellConfig = {
  contentWidth: "wide" as ContentWidth,
  contentPadding: "default" as ContentPadding,
  sidebarWidth: "15rem",
  sidebarStickyOffset: "5.5rem",
}
