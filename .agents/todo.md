# Active Gaps

Keep this list limited to known repository gaps, not speculative features.

## Product

- Implement remaining public routes when their content/design is approved.
- Complete real signed-in workflows and interaction states.
- Add automated UI/interaction tests for stable flows.

## Platform

- Select a production identity provider before wiring auth integration.
- Define backend/data requirements before creating a deployable service.
- Add environment templates only when real integrations require variables.
- Select hosting providers before adding deployment workflows.

## GitHub after first push

- Enable a `main` branch ruleset/required checks.
- Enable Dependabot alerts/security updates.
- Enable CodeQL **Default setup**/code scanning, secret scanning/push protection and private vulnerability reporting where available.
- Expand CODEOWNERS only when real collaborators/teams exist.
